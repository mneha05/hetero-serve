# Core: the scheduler, KV cache and reference engine need only numpy.
numpy>=2.0

# NVIDIA path: CUDA engine + the fused paged-attention kernel.
# Optional. Install the CUDA build for your driver from pytorch.org; the
# kernel is compiled on first use via torch.utils.cpp_extension, which needs
# both nvcc and ninja. Colab ships nvcc but NOT ninja.
torch>=2.4
ninja>=1.11        # build backend for the CUDA extensions

# Intel path: Arc GPU / NPU / optimised CPU.
# Optional. Without either backend everything still runs on the numpy engine.
openvino>=2025.0

# Exact GPT-2 pre-tokenisation (\p{L} classes). Optional; there is a stdlib
# fallback that matches on ASCII.
regex>=2024.0

# Live dashboard only.
fastapi>=0.110
uvicorn>=0.27

# Charts only.
matplotlib>=3.8

# Tests only.
pytest>=8.0
